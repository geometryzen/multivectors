import { Matrix0 } from "./Matrix0";

describe("Matrix0", function () {
    describe("constructor", function () {
        it("", function () {
            const matrix = new Matrix0(new Float32Array([]));
            expect(matrix).toBeDefined();
        });
    });
});
